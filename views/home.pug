extends layouts/main
//- mixins
include mixins/videoBlock

block content
    .videos
        each video in videos
            +videoBlock({
                id: video.id,
                videoFile:video.fileUrl,
                title:video.title,
                views:video.views,
            })